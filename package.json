{
  "name": "cloud-catcher",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "mkdir -p dist && cp index.html styles.css app.js openapi.json dist/ && cp -r src dist/src",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@netlify/blobs": "^9.1.0",
    "sharp": "^0.34.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {"netlify-cli": "^23.6.0"}
}
