{
  "name": "cloud-catcher",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "mkdir -p dist/src && cp index.html styles.css app.js atlas-viewer.js atlas-viewer.css region-selector.js region-selector.css learn-mode.js learn-mode.css openapi.json dist/ && cp src/*.js dist/src/",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@netlify/blobs": "^9.1.0",
    "sharp": "^0.34.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {"netlify-cli": "^23.6.0"}
}
